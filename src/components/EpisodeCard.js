import React from 'react'

const EpisodeCard = () => {
  /*useEffect(() => {
    if (status === 'idle') {
      dispatch(fetchPodcastEpisodes(podcastId));
    }
  }, [status, dispatch, podcastId]);

  useEffect(() => {
    if (status === 'succeeded') {
      localStorage.setItem(`episodes-${podcastId}`, JSON.stringify(episodes));
    }
  }, [status, episodes, podcastId]);*/
  return (
    <div>EpisodeCard</div>
  )
}

export default EpisodeCard